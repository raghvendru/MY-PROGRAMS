#include <stdio.h>

int main(int argc,char*argv[])

{
    int i, n, sum=0;

    
    for(i=1; i<=100; i++)

     if(i%2==0)
{

    {
        sum += i;
    }
}

    printf("Sum of first %d even numbers = %d", n, sum);

    return 0;
}