#include <stdio.h>

int main(int argc,char*argv[])

{
    int i, n, sum=0;

    
    for(i=-20; i<=0; i++)

     if(i%2!=0)
{

    {
        sum += i;
    }
}

    printf("Sum of first %d odd numbers = %d", n, sum);

    return 0;
}