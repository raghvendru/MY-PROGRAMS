#include <stdio.h>

int main(int argc,char*argv[])
{
    int a= 3,b=5;
    int s;
    s = (a+b);
    scanf("%d,%d",&a,&b);
    printf("enter two number\n",s);
    printf("sum of two number is %d\n",s);
}